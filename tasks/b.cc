#include <bits/stdc++.h>
using namespace std;


int main(int argc, char const *argv[])
{
    long long num, n;

    cin >> n;
    while (n--) {
        cin >> num;
        cout << "1 " << num - 1 << endl;
    }
    return 0;
}

